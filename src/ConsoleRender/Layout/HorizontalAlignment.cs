namespace ConsoleRender;

/// <summary>Horizontal alignment inside the parent area (used when no left/right anchor is set).</summary>
public enum HorizontalAlignment
{
    Left,
    Center,
    Right,
}
